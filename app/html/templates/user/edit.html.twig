{% extends 'base.html.twig' %}

{% block header_title %}<h1>Modifier <strong>{{ user.username }}</strong></h1>{% endblock %}

{% block header_img %}{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('user_edit', {'id' : user.id})}) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success pull-left">Modifier</button>
        <a href="{{ path('user_list') }}" class="btn btn-info pull-right">Retour à la gestion des utilisateurs</a>

        {{ form_end(form) }}
    </div>
{% endblock %}
