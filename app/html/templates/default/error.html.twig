{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>Oups, une erreur est survenue.</h1>
{% endblock %}

{% block body %}
    <div class="row m-2">
        {{ message }}

        <div class="d-flex justify-content-end">
            <a href="{{ path('homepage') }}" class="btn btn-info pull-right">Retour à la page d'accueil</a>
        </div>
    </div>
{% endblock %}
